# 🎥 Архитектура видео-плееров

## 📋 Обзор

Платформа поддерживает воспроизведение видео с трех платформ: **YouTube**, **TikTok** и **Instagram**. Все видео-плееры реализованы через простые iframe-встраивания без дополнительных контролов управления.

---

## 🏗️ Архитектура компонентов

### UnifiedVideoPlayer

**Расположение:** `project_x_frontend/src/components/VideoPlayer/UnifiedVideoPlayer.jsx`

**Назначение:** Единый компонент-обертка для всех платформ, который определяет, какой конкретный плеер использовать.

**Параметры:**
- `platform` (string) - платформа: 'youtube', 'tiktok', 'instagram'
- `platformVideoId` (string) - ID видео на платформе
- `sourceUrl` (string) - оригинальная ссылка (используется для Instagram)
- `autoplay` (boolean, default: true) - автозапуск
- `muted` (boolean, default: true) - без звука
- `loop` (boolean, default: false) - зацикливание

**Особенности:**
- Простая реализация без VideoOverlay и useVideoPlayer хука
- Нет кнопок управления (play/pause/mute)
- Видео воспроизводится через нативные контролы платформ (для TikTok)

---

## 📺 Реализация по платформам

### 1. YouTube Player

**Компонент:** `YouTubePlayer.jsx`

**Реализация:** Простой iframe с YouTube Embed API

**URL формат:**
```
https://www.youtube.com/embed/{VIDEO_ID}?controls=0&autoplay=1&mute=0&rel=0&playsinline=1&enablejsapi=1&loop=0
```

**Параметры:**
- `controls=0` - скрыть встроенные контролы
- `autoplay=1` - автозапуск (если передан autoplay=true)
- `mute=0` - **всегда включен звук** для YouTube
- `rel=0` - не показывать рекомендации
- `playsinline=1` - воспроизведение встроено
- `enablejsapi=1` - включить JavaScript API (для будущего расширения)
- `loop=0` - без зацикливания (если loop=true, добавляется playlist параметр)

**Особенности:**
- Звук всегда включен (`mute=0`)
- Для зацикливания используется параметр `playlist={VIDEO_ID}`

---

### 2. TikTok Player

**Компонент:** `TikTokPlayer.jsx`

**Реализация:** Простой iframe с TikTok Player v1 API

**URL формат:**
```
https://www.tiktok.com/player/v1/{VIDEO_ID}?autoplay=1&loop=0&muted=0&controls=1&description=0&music_info=0&rel=0
```

**Параметры:**
- `autoplay=1` - автозапуск (если передан autoplay=true)
- `loop=1` - зацикливание (если передан loop=true)
- `muted=0` - **всегда включен звук** для TikTok
- `controls=1` - **показать контролы** (нативные контролы TikTok)
- `description=0` - скрыть описание
- `music_info=0` - скрыть информацию о музыке
- `rel=0` - не показывать рекомендации

**Особенности:**
- Используется **player/v1** API (не embed/v2)
- Звук всегда включен (`muted=0`)
- Контролы отображаются (`controls=1`)

---

### 3. Instagram Player

**Компонент:** `InstagramPlayer.jsx`

**Реализация:** Использует официальный Instagram Embed.js

**Метод:**
- Создается `<blockquote>` элемент с классом `instagram-media`
- Загружается скрипт `https://www.instagram.com/embed.js`
- Instagram автоматически обрабатывает embed

**Параметры:**
- `data-instgrm-permalink` - оригинальная ссылка (sourceUrl)
- `data-instgrm-version` - версия API (14)

**Особенности:**
- Используется `source_url` из базы данных
- Instagram сам управляет воспроизведением
- Нет программного управления

---

## 🎬 Использование в компонентах

### VideoCard (список видео)

**Расположение:** `project_x_frontend/src/components/VideoCard/VideoCard.jsx`

**Особенности:**
- **Lazy loading** через Intersection Observer API
- Видео загружается только при попадании в viewport (с запасом 50px)
- Приоритет отображения:
  1. Активное видео (если есть `platform` и `platform_video_id`) - через UnifiedVideoPlayer
  2. Preview изображение (`preview_url`)
  3. Placeholder с иконкой платформы

**Параметры для UnifiedVideoPlayer в VideoCard:**
- `autoplay={isVisible}` - автозапуск только если видно в viewport
- `muted={true}` - без звука в списке
- `loop={true}` - зацикливание в списке

**Lazy Loading:**
```javascript
// Intersection Observer с запасом 50px
rootMargin: '50px',
threshold: 0.1
```

---

### VideoDetailView (детальная страница)

**Расположение:** `project_x_frontend/src/components/VideoDetailView/VideoDetailView.jsx`

**Особенности:**
- Приоритет: активное видео → preview_url → placeholder
- Параметры для UnifiedVideoPlayer:
  - `autoplay={true}` - автозапуск
  - `muted={true}` - без звука
  - `loop={false}` - без зацикливания

---

## 📐 Стилизация

### UnifiedVideoPlayer.css

**Aspect Ratio:** 9:16 (вертикальное видео)
- `padding-bottom: 177.78%` - для поддержания пропорций

**Адаптивность:**
- **Мобильные** (< 480px): полная ширина, без скругления
- **Планшеты** (481-768px): max-width 380px
- **Планшеты ландшафт** (769-1024px): max-width 420px
- **Средние экраны** (1025-1440px): max-width 460px
- **Большие экраны** (> 1441px): max-width 500px

**Отступы:**
- Вертикальные: 0.5cm сверху и снизу
- Горизонтальные: 5mm слева и справа

---

## 🔧 Технические детали

### Нормализация URL

**Сервис:** `PlatformNormalizationService.php`

**Функция:** Автоматическое определение платформы и извлечение video ID из URL

**Поддерживаемые форматы:**

**YouTube:**
- `youtube.com/watch?v={ID}`
- `youtu.be/{ID}`
- `youtube.com/shorts/{ID}`
- `youtube.com/embed/{ID}`
- `m.youtube.com/watch?v={ID}`

**TikTok:**
- `tiktok.com/@username/video/{ID}`
- `vm.tiktok.com` (с разрешением редиректа)
- `t.tiktok.com` (с разрешением редиректа)
- `m.tiktok.com/v/{ID}`

**Instagram:**
- `instagram.com/p/{ID}`
- `instagram.com/reel/{ID}`
- `instagram.com/tv/{ID}`

**Результат нормализации:**
- `platform` - название платформы
- `platform_video_id` - ID видео
- `normalized` - флаг успешности

---

## 📊 Структура данных

### Поле `platform_video_id` в БД

**Таблица:** `video_references`
**Тип:** `string(255), nullable`
**Назначение:** Хранит ID видео на платформе после нормализации URL

**Миграция:** `2026_01_09_202621_add_platform_video_id_to_video_references_table.php`

---

## 🎯 Логика работы

### Приоритет отображения видео

1. **Если есть `platform` и `platform_video_id`:**
   - Используется UnifiedVideoPlayer с активным видео

2. **Если есть `preview_url`:**
   - Показывается статичное изображение

3. **Иначе:**
   - Показывается placeholder с иконкой платформы

### Автозапуск и звук

**В списке (VideoCard):**
- `autoplay={isVisible}` - только если видно
- `muted={true}` - без звука
- `loop={true}` - зацикливание

**На детальной странице (VideoDetailView):**
- `autoplay={true}` - всегда
- `muted={true}` - без звука
- `loop={false}` - без зацикливания

**Важно:** Параметры `muted` в компонентах плееров игнорируются для YouTube и TikTok - звук всегда включен (`mute=0` / `muted=0`).

---

## 🚀 Производительность

### Lazy Loading

- Видео загружается только при приближении к viewport
- Запас: 50px до появления
- При уходе из viewport загрузка останавливается

### Оптимизация

- Используется Intersection Observer API (нативный браузерный API)
- Задержка 200ms перед загрузкой для оптимизации
- Остановка загрузки при скролле

---

## 📝 Примечания

1. **Нет программного управления:** Все плееры работают через простые iframe без дополнительных контролов
2. **Звук всегда включен:** Для YouTube и TikTok звук всегда включен, параметр `muted` не влияет
3. **TikTok контролы:** TikTok показывает свои нативные контролы (`controls=1`)
4. **Lazy loading:** Видео в списке загружаются только при видимости
5. **Простая архитектура:** Нет VideoOverlay, useVideoPlayer хука и других сложных механизмов управления

---

## 🔄 История изменений

- **Текущая версия:** Простые iframe без контролов
- **Удалено:** VideoOverlay, useVideoPlayer, кнопки управления
- **Добавлено:** Lazy loading в VideoCard, platform_video_id в БД
- **Изменено:** TikTok использует player/v1 с controls=1 и muted=0

# 🎥 Архитектура видео-плееров

## 📋 Обзор

Платформа поддерживает воспроизведение видео с четырех платформ: **YouTube**, **TikTok**, **Instagram** и **Facebook**. Все видео-плееры реализованы через простые iframe-встраивания. Архитектура разделена на два специализированных компонента для разных случаев использования: список видео и детальная страница.

---

## 🏗️ Архитектура компонентов

### usePlatformPlayer (хук)

**Расположение:** `project_x_frontend/src/components/VideoPlayer/usePlatformPlayer.js`

**Назначение:** Хук с общей логикой выбора платформы и рендеринга соответствующего плеера.

**Параметры:**
- `platform` (string) - платформа: 'youtube', 'tiktok', 'instagram', 'facebook'
- `platformVideoId` (string) - ID видео на платформе
- `sourceUrl` (string) - оригинальная ссылка (используется для Instagram и Facebook)

**Возвращает:**
- `renderPlayer(playerProps)` - функция для рендеринга плеера с заданными параметрами

**Особенности:**
- Единая точка выбора платформы
- Используется обоими компонентами (VideoListPlayer и VideoDetailPlayer)
- Переиспользуемая логика без дублирования кода

---

### VideoListPlayer (для списка видео)

**Расположение:** `project_x_frontend/src/components/VideoPlayer/VideoListPlayer.jsx`

**Назначение:** Компонент для отображения видео в каталоге (списке).

**Параметры:**
- `platform` (string) - платформа: 'youtube', 'tiktok', 'instagram', 'facebook'
- `platformVideoId` (string) - ID видео на платформе
- `sourceUrl` (string) - оригинальная ссылка (используется для Instagram и Facebook)
- `isVisible` (boolean, default: false) - видимость видео в viewport

**Предустановленные параметры:**
- `autoplay={isVisible}` - автозапуск только если видно в viewport
- `muted={true}` - без звука
- `loop={true}` - с зацикливанием
- `controls={false}` - без контролов

**Особенности:**
- Оптимизирован для списка видео
- Lazy loading через Intersection Observer
- Минимальные контролы для лучшего UX в каталоге

---

### VideoDetailPlayer (для детальной страницы)

**Расположение:** `project_x_frontend/src/components/VideoPlayer/VideoDetailPlayer.jsx`

**Назначение:** Компонент для отображения видео на детальной странице.

**Параметры:**
- `platform` (string) - платформа: 'youtube', 'tiktok', 'instagram', 'facebook'
- `platformVideoId` (string) - ID видео на платформе
- `sourceUrl` (string) - оригинальная ссылка (используется для Instagram и Facebook)

**Предустановленные параметры:**
- `autoplay={true}` - всегда автозапуск
- `muted={false}` - со звуком
- `loop={false}` - без зацикливания
- `controls={true}` - с контролами
- `showText={false}` - для Facebook: не показывать текст поста

**Особенности:**
- Оптимизирован для детального просмотра
- Полный контроль над воспроизведением
- Звук включен по умолчанию

---

## 📺 Реализация по платформам

### 1. YouTube Player

**Компонент:** `YouTubePlayer.jsx`

**Реализация:** Простой iframe с YouTube Embed API

**URL формат:**
```
https://www.youtube.com/embed/{VIDEO_ID}?controls={0|1}&autoplay={0|1}&mute={0|1}&rel=0&playsinline=1&enablejsapi=1&loop={0|1}
```

**Параметры:**
- `controls` - показывать/скрывать контролы (зависит от пропса)
- `autoplay` - автозапуск (зависит от пропса)
- `mute` - звук (0 = включен, 1 = выключен, зависит от пропса `muted`)
- `rel=0` - не показывать рекомендации
- `playsinline=1` - воспроизведение встроено
- `enablejsapi=1` - включить JavaScript API (для будущего расширения)
- `loop` - зацикливание (если loop=true, добавляется playlist параметр)

**Особенности:**
- Поддержка параметра `controls` (можно включать/выключать)
- Поддержка параметра `muted` (правильная обработка звука)
- Для зацикливания используется параметр `playlist={VIDEO_ID}`

---

### 2. TikTok Player

**Компонент:** `TikTokPlayer.jsx`

**Реализация:** Простой iframe с TikTok Player v1 API

**URL формат:**
```
https://www.tiktok.com/player/v1/{VIDEO_ID}?autoplay={0|1}&loop={0|1}&muted={0|1}&controls={0|1}&description=0&music_info=0&rel=0
```

**Параметры:**
- `autoplay` - автозапуск (зависит от пропса)
- `loop` - зацикливание (зависит от пропса)
- `muted` - звук (0 = включен, 1 = выключен, зависит от пропса)
- `controls` - показывать/скрывать контролы (зависит от пропса)
- `description=0` - скрыть описание
- `music_info=0` - скрыть информацию о музыке
- `rel=0` - не показывать рекомендации

**Особенности:**
- Используется **player/v1** API (не embed/v2)
- Поддержка параметра `controls` (можно включать/выключать)
- Поддержка параметра `muted` (правильная обработка звука)

---

### 3. Instagram Player

**Компонент:** `InstagramPlayer.jsx`

**Реализация:** Использует официальный Instagram Embed.js

**Метод:**
- Создается `<blockquote>` элемент с классом `instagram-media`
- Загружается скрипт `https://www.instagram.com/embed.js`
- Instagram автоматически обрабатывает embed

**Параметры:**
- `data-instgrm-permalink` - оригинальная ссылка (sourceUrl)
- `data-instgrm-version` - версия API (14)

**Особенности:**
- Используется `source_url` из базы данных
- Нормализует URL (убирает query параметры типа `?igsh=...`)
- Добавляет `data-instgrm-captioned="true"` для inline playback
- Instagram сам управляет воспроизведением
- Нет программного управления
- Не поддерживает параметры autoplay, muted, loop, controls

---

### 4. Facebook Player

**Компонент:** `FacebookPlayer.jsx`

**Реализация:** Использует официальный Facebook Video Plugin через iframe

**URL формат:**
```
https://www.facebook.com/plugins/video.php?href={ENCODED_URL}&show_text={0|1}&width=400
```

**Параметры:**
- `sourceUrl` - оригинальная ссылка на Facebook видео
- `showText` - показывать ли текст поста (0 или 1, по умолчанию false)

**Поддерживаемые форматы URL:**
- `facebook.com/reel/{ID}` - Reels
- `facebook.com/watch/?v={ID}` - Watch видео
- `facebook.com/{user}/videos/{ID}/` - обычные видео
- `facebook.com/{user}/posts/{ID}` - посты с видео

**Iframe атрибуты:**
- `width="400"` (HTML атрибут, обязателен для Facebook API)
- `height="700"` (HTML атрибут)
- `style="border:none;overflow:hidden"`
- `scrolling="no"`
- `frameborder="0"`
- `allow="autoplay; clipboard-write; encrypted-media; picture-in-picture; web-share"`
- `allowfullscreen="true"`

**Особенности:**
- Нормализует URL (убирает query параметры, кроме `/watch/?v=`)
- Использует `encodeURIComponent` для кодирования URL
- `width=400` в параметрах URL (число в пикселях, не процент!)
- CSS стили `width: 100%` и `height: 100%` для адаптивности
- Не требует Meta App, токенов или API

---

## 🎬 Использование в компонентах

### VideoCard (список видео)

**Расположение:** `project_x_frontend/src/components/VideoCard/VideoCard.jsx`

**Особенности:**
- **Lazy loading** через Intersection Observer API
- Видео загружается только при попадании в viewport (с запасом 50px)
- Приоритет отображения:
  1. Активное видео (если есть `platform` и `platform_video_id`) - через VideoListPlayer
  2. Placeholder с иконкой платформы

**Использование VideoListPlayer:**
```jsx
<VideoListPlayer
  platform={video.platform}
  platformVideoId={video.platform_video_id}
  sourceUrl={video.source_url}
  isVisible={isVisible}  // Только если видно в viewport
/>
```

**Lazy Loading:**
```javascript
// Intersection Observer с запасом 50px
rootMargin: '50px',
threshold: 0.1
```

---

### VideoDetailView (детальная страница)

**Расположение:** `project_x_frontend/src/components/VideoDetailView/VideoDetailView.jsx`

**Особенности:**
- Приоритет: активное видео → placeholder
- Всегда загружается (нет lazy loading)

**Использование VideoDetailPlayer:**
```jsx
<VideoDetailPlayer
  platform={video.platform}
  platformVideoId={video.platform_video_id}
  sourceUrl={video.source_url}
/>
```

---

## 📐 Стилизация

### VideoListPlayer.css

**Назначение:** Стили для видео в списке (каталоге)

**Особенности:**
- Без отступов (`margin: 0`, `padding: 0`)
- Без скругления (`border-radius: 0`)
- Без тени (`box-shadow: none`)
- Занимает 100% ширины и высоты контейнера
- `padding-bottom: 0` (переопределяет базовые стили)

---

### VideoDetailPlayer.css

**Aspect Ratio:** 9:16 (вертикальное видео)
- `padding-bottom: 177.78%` - для поддержания пропорций

**Адаптивность:**
- **Мобильные** (< 480px): полная ширина, без скругления
- **Планшеты** (481-768px): max-width 380px
- **Планшеты ландшафт** (769-1024px): max-width 420px
- **Средние экраны** (1025-1440px): max-width 460px
- **Большие экраны** (> 1441px): max-width 500px

**Отступы:**
- Вертикальные: 0.5cm сверху и снизу
- Горизонтальные: 5mm слева и справа

---

## 🔧 Технические детали

### Нормализация URL

**Сервис:** `PlatformNormalizationService.php`

**Функция:** Автоматическое определение платформы и извлечение video ID из URL

**Поддерживаемые форматы:**

**YouTube:**
- `youtube.com/watch?v={ID}`
- `youtu.be/{ID}`
- `youtube.com/shorts/{ID}`
- `youtube.com/embed/{ID}`
- `m.youtube.com/watch?v={ID}`

**TikTok:**
- `tiktok.com/@username/video/{ID}`
- `vm.tiktok.com` (с разрешением редиректа)
- `t.tiktok.com` (с разрешением редиректа)
- `m.tiktok.com/v/{ID}`

**Instagram:**
- `instagram.com/p/{ID}`
- `instagram.com/reel/{ID}`
- `instagram.com/tv/{ID}`

**Facebook:**
- `facebook.com/reel/{ID}`
- `facebook.com/watch/?v={ID}`
- `facebook.com/{user}/videos/{ID}/`
- `facebook.com/{user}/posts/{ID}`

**Результат нормализации:**
- `platform` - название платформы
- `platform_video_id` - ID видео
- `normalized` - флаг успешности

---

## 📊 Структура данных

### Поле `platform_video_id` в БД

**Таблица:** `video_references`
**Тип:** `string(255), nullable`
**Назначение:** Хранит ID видео на платформе после нормализации URL

**Миграция:** `2026_01_09_202621_add_platform_video_id_to_video_references_table.php`

---

## 🎯 Логика работы

### Приоритет отображения видео

1. **Если есть `platform` и `platform_video_id`:**
   - Используется VideoListPlayer (в списке) или VideoDetailPlayer (на детальной странице)

2. **Иначе:**
   - Показывается placeholder с иконкой платформы

### Автозапуск и звук

**В списке (VideoCard → VideoListPlayer):**
- `autoplay={isVisible}` - только если видно в viewport
- `muted={true}` - без звука
- `loop={true}` - с зацикливанием
- `controls={false}` - без контролов

**На детальной странице (VideoDetailView → VideoDetailPlayer):**
- `autoplay={true}` - всегда
- `muted={false}` - со звуком
- `loop={false}` - без зацикливания
- `controls={true}` - с контролами

---

## 🚀 Производительность

### Lazy Loading

- Видео загружается только при приближении к viewport
- Запас: 50px до появления
- При уходе из viewport загрузка останавливается
- Используется только в VideoListPlayer (список)

### Оптимизация

- Используется Intersection Observer API (нативный браузерный API)
- Задержка 200ms перед загрузкой для оптимизации
- Остановка загрузки при скролле

---

## 📝 Примечания

1. **Разделение компонентов:** Два специализированных компонента для разных случаев использования (список и детальная страница)
2. **Гибкость параметров:** YouTube и TikTok поддерживают параметры `controls` и `muted`
3. **Instagram ограничения:** Instagram не поддерживает программное управление параметрами
4. **Lazy loading:** Видео в списке загружаются только при видимости
5. **Переиспользование логики:** Хук `usePlatformPlayer` исключает дублирование кода

---

## 🔄 История изменений

- **2026-01-12:** Добавлена поддержка Facebook
  - Создан `FacebookPlayer.jsx` с использованием официального Facebook Video Plugin
  - Обновлен `usePlatformPlayer.js` для поддержки Facebook
  - Обновлен `PlatformNormalizationService` для извлечения Facebook video ID
  - Добавлен `FACEBOOK` в `PlatformEnum`
  - Обновлена нормализация URL для Facebook (удаление query параметров)
- **2026-01-XX:** Разделение на VideoListPlayer и VideoDetailPlayer
  - Создан хук `usePlatformPlayer.js` для общей логики выбора платформы
  - Создан `VideoListPlayer.jsx` для списка (muted, loop, no controls)
  - Создан `VideoDetailPlayer.jsx` для детальной страницы (unmuted, no loop, controls)
  - Обновлены `YouTubePlayer.jsx` и `TikTokPlayer.jsx` (поддержка controls и muted)
  - Улучшена нормализация URL для Instagram (удаление query параметров, добавление `data-instgrm-captioned`)
  - Удален `UnifiedVideoPlayer.jsx` (заменен на специализированные компоненты)
- **Ранее:** Простые iframe без контролов, UnifiedVideoPlayer для всех случаев
